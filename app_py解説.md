# ✍️ AIライティングツールを支える「玄関ページ」を読み解く

## この記事について
Streamlit製の個人用AIライティングツールのエントリーポイント `app.py`（全29行）を、初心者向けに解説します。

## 1. インポートと環境変数の読み込み

```python
import os
import streamlit as st
from dotenv import load_dotenv

load_dotenv()
```

- `streamlit`：Pythonだけでブラウザ上のWebアプリを作れるライブラリ。
- `python-dotenv`：`.env` ファイルに書いた設定値（APIキーなど）を読み込む定番ツール。
- `load_dotenv()` を呼ぶことで、同じフォルダにある `.env` ファイルの中身が環境変数として使えるようになります。APIキーをコードに直書きせずに済むための工夫です。

## 2. ページの基本設定

```python
st.set_page_config(page_title="AIライティングツール", page_icon="✍️", layout="wide")
```

ブラウザのタブに表示されるタイトルとアイコン（✍️）、そして画面を横幅いっぱいに使う `layout="wide"` を指定しています。この関数は各ページで一番最初に呼ぶ必要がある、Streamlit独自のルールです。

## 3. トップページの見出しと説明文

```python
st.title("✍️ AIライティングツール")
st.write("個人用のAI文章作成アシスタントです。左のメニューから機能を選んでください。")

st.markdown(
    """
- **ブログ記事執筆** — テーマとキーワードから記事を生成
- **メール返信作成** — 受信メールから返信文を生成
- **文章要約** — 長文を要約
- **文章校正/リライト** — トーン調整や誤字脱字の修正
"""
)
```

ここは単なる案内文です。実際の機能は `pages/` フォルダの中の各ファイル（`1_ブログ記事執筆.py` など）が担当していて、Streamlitは番号付きファイル名を自動でサイドバーのメニューに変換してくれます。つまり `app.py` 自体は「表紙・玄関」であり、各機能の実装はここには一切ありません。

## 4. APIキーが設定されているかのチェック

```python
if not os.getenv("GEMINI_API_KEY"):
    st.warning(
        "GEMINI_API_KEY が設定されていません。プロジェクト直下に `.env` ファイルを作成し、"
        "`GEMINI_API_KEY=あなたのAPIキー` を記入してください（`.env.example` を参考にしてください）。"
    )
else:
    st.success("Gemini API キーが読み込まれています。")
```

- `os.getenv("GEMINI_API_KEY")` で環境変数からGeminiのAPIキーを取得。
- 取得できなければ黄色い警告 (`st.warning`)、設定済みなら緑の成功メッセージ (`st.success`) を表示します。
- これにより、他の機能ページに行く前に「APIキー未設定」という初心者がハマりがちなミスに、トップ画面で気づけるようになっています。

## まとめ

`app.py` は次の3つの役割だけを持つ、非常にシンプルなファイルです。

1. `.env` からAPIキーを読み込む
2. アプリ全体のタイトル・レイアウトと、各機能への案内を表示する
3. Gemini APIキーが設定されているかをチェックし、ユーザーに知らせる

実際の「記事生成」「メール返信」などのロジックは `pages/` 配下と `utils/gemini_client.py` にあります。気になる場合はそちらも読んでみると、Gemini APIの呼び出し方が分かります。
